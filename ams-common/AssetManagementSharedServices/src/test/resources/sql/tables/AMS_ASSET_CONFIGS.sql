-- AMS_ASSET_CONFIGS
CREATE TABLE AMS_ASSET_CONFIGS (
  CONFIG_ID             NUMBER(19) NOT NULL PRIMARY KEY,
  ASSET_ID              NUMBER(19),
  ORDER_ID              NUMBER(19),
  CONFIG_TYPE_CD        VARCHAR(20),
  CONFIG_STATUS_CD      VARCHAR(20),
  NETWORK_CONFIG_CD     VARCHAR(20),
  DATA_SOURCE_CD        VARCHAR(20) DEFAULT 'AMS',
  LAN_IP_ADDRESS        VARCHAR(15),
  LAN_SUBNET_MASK       VARCHAR(15),
  WAN_IP_ADDRESS        VARCHAR(15),
  WAN_SUBNET_MASK       VARCHAR(15),
  DEFAULT_GATEWAY       VARCHAR(15),
  LAN_GATEWAY           VARCHAR(15),
  PRIMARY_DNS           VARCHAR(15),
  SECONDARY_DNS         VARCHAR(15),
  CIRCUIT_ID            VARCHAR(60),
  BANDWIDTH_KBPS        NUMBER(10),
  EFFECTIVE_DT          TIMESTAMP,
  REVISION_NUM          NUMBER(10),
  CREATED_DT            TIMESTAMP,
  CREATED_BY            VARCHAR(64),
  MODIFIED_DT           TIMESTAMP,
  MODIFIED_BY           VARCHAR(64)
);
