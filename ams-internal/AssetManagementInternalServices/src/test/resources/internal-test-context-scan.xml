<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

  <!--
    Scans both the shared library and the internal module, which is what the deployed application
    does too - so a bean name that collides between the two shows up here rather than at start-up.
  -->
  <context:annotation-config/>
  <context:component-scan base-package="org.example.am"/>

  <bean id="restLogger" class="org.example.am.shared.utils.RestLogger"/>
  <bean id="restTemplate" class="org.springframework.web.client.RestTemplate"/>

</beans>
