# Filtered at build time; read by ApplicationVersionListener and rendered in the page footer.
application.name=AMS Internal Asset Management
application.version=${project.version}
application.build=${buildNumber}
